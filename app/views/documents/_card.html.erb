<div class="doc">
  <div class="pic">
    <% if document.file.attached? %>
      <%= cl_image_tag document.file.key, height: 300, width: 400, crop: :fill %>
    <% end %>
  </div>
  <div class="info">
    <h2><%= document.title %></h2>
    <p><%= document.description %></p>
  </div>
</div>
